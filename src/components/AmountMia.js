import React from 'react';

// TODO: wire up a real balance!

export function AmountMia({ mia, className }) {
  return (
    <span className={className}>
      1,250,000
      &nbsp;$MIA
    </span>
  );
}
